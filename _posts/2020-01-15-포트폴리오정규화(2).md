---
title: "Optimal Portfolio Selection using Regularization를 읽고 (2)(작성중)"
excerpt: "포트폴리오 정규화 방법"
toc: true
toc_sticky: true
use_math: true

author_profile: true

date: 2020-01-15 15:00:00 -0000

categories: 
  - 논문 읽기
tags:
  - 포트폴리오 최적화
---

### <span style="color:#F7DC6F;">Optimal Portfolio Selection using Regularization</span>
--------------
이 논문은 기존의 Markowitz(mean-variance) 모델에서 covariance matrix의 추정에서 생기는 오차를 줄이고자 사용하는 _정규화(regularization)_ 방법을 소개한다.  그리고 기본적인 포트폴리오 이론도 앞에서 설명한다. 

### <span style="color:#00ADB5;"> 3 Regularization as approximation to an inverse problem</span>

이전 포스팅 ["Optimal Portfolio Selection using Regularization를 읽고(1)"](https://chulhongsung.github.io/%EB%85%BC%EB%AC%B8%20%EC%9D%BD%EA%B8%B0/%ED%8F%AC%ED%8A%B8%ED%8F%B4%EB%A6%AC%EC%98%A4%EC%A0%95%EA%B7%9C%ED%99%94(1)/)에 이어서 다음 내용에 대해서 설명하겠다. 먼저 시작에 앞서 사용할 notation에 대해서 설명한다.

### 3.2* SVD(Singular Vector Decomposition)

$(\lambda_j, u_j, v_j), j = 1,\dots,N$을 singular system이라고 하자. 일반적으로 $R$의 singular system이라고 하면 다음을 만족한다.

$$
R = U\Sigma V^\top
$$  

그리고 $U=(u_1, \dots, u_N), \Sigma=diag(\lambda_1, \dots,\lambda_N), V=(v_1,\dots, v_N)$이다.

$U$는  $RR^\top$의 고윳값분해(eigendecomposition)을 통해 나온 고유벡터(eigen vector)를 열벡터로 갖는 행렬이고 $V$는 $R^\top R$을 $U$와 똑같이 고윳값분해를 통해 나온 행렬이다. $\Sigma$는 특잇값(singualr value)들을, 여기서는 $\lambda_j, j = 1,\dots, N$ 대각 원소로 갖는 행렬이다.

즉, 
$$
RR^\top = U(\Sigma\Sigma^\top)U^\top \\
R^\top R = V(\Sigma^\top\Sigma)V^\top \\
$$

하지만, 이 논문에서는 일반적인 $R$의 singular system이 아니다. SVD를 할 때, $R^\top R/T$를 고윳값분해를 통해 $U, \Sigma, V$를 얻는다.

즉, 

$$
Ru_j= \lambda_j v_j \\
R^\top /T v_j = \lambda_j u_j
$$

나중에 고윳값분해와 특잇값분해에 대해서 다루어보겠다.

### <span style="color:#3498DB;">3.2 Ridge regularization</span>

$\textbf{1}_T = R\beta + u$의 회귀문제에서 손실함수(Loss function)에 Ridge 정규화를 추가하면 다음과 같이 쓸 수 있다. 여기서 $u$는 error 항이다. 앞서 언급한 singular vector $u_j$가 아니다.

$$
L(\beta) = (\textbf{1}_T- R\beta)^\top(\textbf{1}_T- R\beta) + \rho\|\beta\|^2
$$

기존의 MSE(Mean Squared Error)에서$\beta$의 $l_2$ norm의 제곱항을 추가했다.

$L(\beta)$를 최소화하는 $\beta$는 미분을 통해 쉽게 찾을 수 있는데 다음과 같다.

$$
\begin{aligned}
\hat{\beta}_{ridge} &= (R^\top R + \rho I)^{-1}R^\top \textbf{1}_T \\
&= (\frac{R^\top R}{T} + \tau I)^{-1}\frac{R^\top\textbf{1}_T}{T},
\end{aligned}
$$

여기서 $\tau = \rho/T$이다.

위의 식에서 앞에서 설명한 SVD의 형태
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTE1ODM5MTgzNzYsLTkyMDY2ODg4OSwxOT
Q2NzYwMTExLC01NjI1NzU4NTMsNjE4MjQ0NzUxLC02NDM2NzI3
ODksMTUzNTk0MjcxNSw1MzkzMTgwODcsLTExNjE3ODI5NjUsLT
ExNjg4NjMzOTksLTg4MjQyNTczNCwtMzM5MDQwMjA4XX0=
-->