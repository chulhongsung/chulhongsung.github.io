---
title:  "다중비교문제(multiple comparison problem)와 해결법"
excerpt: 통계학에서 고차원의 데이터를 가지고 다중검정을 할 때에 생기는 문제와 그 해결법으로 사용하는 본페로니 교정과 FDR에 대해서 소개 

toc: true
toc_sticky: true
use_math: true

author_profile: true

date: 2021-04-07 13:00:00 
categories: 
  - Statistics
  
tags:
  - 다중비교문제
  - 검정
---
이번 포스팅에서 내용과 예시는 'Bradley Efron' 교수님께서 쓰신 "Large-Scale Inference"와 "Computer Age Statistical Inference: Algorithms, Evidence and Data Science"를 참고하였습니다.

## <span style="color:#00ADB5;"> 다중비교문제(Multiple Comparison Problem)</span>

통계학에서 정말 중요한 부분 중 하나가 바로 검정이다. 학부과정에서 수리통계학 수업을 들으면 검정 파트를 정말 자세하고 큰 비중을 두어서 배우게 


### <span style="color:#7194D0;"> 1. Magic method: $\textsf{call(self, input)}$</span>

$\textsf{tf.keras.models.Model}$ 클래스의 $\textsf{call}$ 메소드는 받을 수 있는 인자는 두 가지이다. $\textsf{input}$과 $\textsf{trainging}$이다.  

```python
class Mymodel(tf.keras.models.Model):
	def __init__(self):
		super().__init__()
		self.dense = tf.keras.layers.Dense(1)
		
	def call(self, input, training=True):
		output = self.dense(input)
		return output		
```

만약, $\textsf{input1, input2}$가 존재해서 $\textsf{Mymodel.fit(input1, input2, y)}$ 으로 사용하면 에러가 발생한다. 그래서 그 해결법은 $\textsf{input1, input2}$를 tuple로 주는 것이다. 아래처럼 코드를 작성하면 에러가 발생하지 않는다.

```python
class Mymodel(tf.keras.models.Model):
	def __init__(self):
		super().__init__()
		self.dense = tf.keras.layers.Dense(1)
		
	def call(self, input, training=True):
		input1, input2 = input
		output1 = self.dense(input1)
		output2 = self.dense(input2)
		return output1 + output2
```

$\textsf{Mymodel.fit((input1, input2), y)}$을 사용해서 모델을 학습하면 된다.
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTEzNDk0NjI0NjFdfQ==
-->