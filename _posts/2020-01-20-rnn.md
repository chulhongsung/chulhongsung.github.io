---
title: "RNN(작성중)"
excerpt: "RNN"
bibliography: rnn.bib

toc: true
toc_sticky: true
use_math: true

author_profile: true

date: 2020-01-15 15:00:00 -0000

categories: 
  - 딥러닝

tags:
  - 자연어처리
---

### <span style="color:#F7DC6F;">Recurrent Neural Network</span>
--------------

최근 "seq2seq(sequence-to-sequence)"를 공부할 기회가 생겼다. 그래서 자연어처리에 사용하는 neural network 모델들을 처음부터 정리할 겸 포스팅을 준비했다. 순서는 RNN -> LSTM와 GRU -> seq2seq -> attention으로 이어질 계획이다. 참고자료는 아래에 표기해놓았다.

### <span style="color:#00ADB5;"> 1. 언어모델(Language Model) </span>
-----------

언어 모델은 단어 나열(sequence) 혹은 문장에 확률을 부여한다.  다음과 같이 단어의 나열을 생각해 볼 수 있다. 

$$
w_1, w_2, \dots, w_t
$$

여기서 $w_i, i=1,\dots,t$는 단어를 의미한다. 

예를 들어 "나는 밥을 먹는다"에서 띄어쓰기로 단어를 구분한다는 가정하에 $w_1$="나는", $w_2$="밥을", $w_3$="먹는다"로 대응시킬 수 있다. 

그리고 "나는 밥을 먹는다"와 같은 문장이 나올 확률을 정의할 수 있다. 물론 여기서 단어의 순서는 상관없고 단어들이 동시에 나오는 확률이라고 해서 "동시 확률"이라고 한다.

$$
P(w_1, w_2, \dots, w_t) = P(w_2, w_1, \dots, w_t) =\cdots = P(w_t, w_{t-1}, \dots, w_1)
$$

그리고 위의 확률들을 확률의 곱셈정리에 의해 다음과 같이 표현이 가능하다.

$$
P(w_1, w_2, \dots, w_t) = \prod_{i=1}^{t}P(w_i |w_1,\dots, w_{i-1} )
$$

언어모델 중 word2vec 방법들은 동시 확률을 이용해서 단어들의 임베딩 벡터를 학습했다. 임베딩 벡터 학습에는 큰 문제가 없을 수 있지만 만약 문장을 만드는 모델에서는 단어의 순서를 고려하지 않는다면 큰 문제가 발생할 수 있다. 따라서 단어의 순서를 고려할 수 있는 모델들을 고려하게 된다. 그 중에 하나가 바로 __"RNN(Recurrent Neural Network)"__ 이다. 

### <span style="color:#00ADB5;">2. RNN(Recurrent Neural Network)</span>
-----------

### 2.1 Recurrent? 
RNN을 번역하면 순환신경망이라고 한다. 무엇이 순환하는건가? 바로 뒤에서 설명할 __"은닉 상태(hidden state)"__ 이 신경망 모델에서 순환하게 된다. 먼저 그림을 통해 확인해보자. 

![enter image description here](https://www.researchgate.net/profile/Weijiang_Feng/publication/318332317/figure/fig1/AS:614309562437664@1523474221928/The-standard-RNN-and-unfolded-RNN.png)

위의 그림은 "Audio visual speech recognition with multimodal recurrent neural networks(Feng et al, 2017)"에서 따온 그림이다. 

먼저 왼쪽 그림을 보면 입력값 $x$가 모델로 들어가서 연산을 통해 은닉 상태 $h$가 되는데 $h$는 다시 모델로 들어가도록 설계가 되어있는 것을 확인할 수 있다. 개인적으로 오른쪽에 있는 "Unfold 그림"을 더 선호하는데 그 이유가 모델을 펼쳐보면 더 이해가 잘 된다. 

$x^{(t)}$는 여기서 $t$ 시점의 입력값이다. $t$ 시점의 입력값 $x^{(t)}$이 신경망 모델로 들어가게 되는데 __이때, $(t-1)$시점의 은닉 상태 $h^{(t-1)}$도 같이 모델로 들어가게 된다__. 여기서 $h^{(t-1)}$는 이전 $(t-1)$ 시점에서 $x^{(t-1)}$과 $h^{(t-2)}$으로 만들어진 것이다. 즉, 이전 정보가 들어있는 은닉 상태이다. 바로 이전 시점의 은닉 상태가 입력값으로 들어오기 때문에 __"이전의 정보"__ 가 사용되는 것이다. 물론 $h^{(t-1)}$은 바로 이전 $(t-1)$ 시점의 은닉 상태이긴하지만 이전 $t-1,\dots, 1$ 시점들의 모든 정보를 포함하고 있다. 그리고 은닉(hidden)이라고 하는 이유는 모델의 최종 출력값으로 나오지않기 때문이라고 생각한다. 코드를 통해서는 중간에 은닉 상태 값을 확인할 수 있다.

이제 은닉 상태 $h^{(t)}$를 어떻게 계산하는지 알아보도록 하자.

### 2.2 Hidden state

### <span style="color:#00ADB5;"> RNN의 한계</span>
-----------
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTE5NDMzMDcyNjUsMTIyMzI1MzQ3NCwxMz
Y4MTY0Mjk2LDM2MzgzMzM2N119
-->