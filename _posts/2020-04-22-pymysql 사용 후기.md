---
title: "pymysql 사용기"
excerpt: "pymysql"

toc: true
toc_sticky: true
use_math: true

author_profile: true

date: 2020-04-22 19:00:00 -0000

categories: 
  - python

tags:
  - pymysql
---

### <span style="color:#F7DC6F;">pymysql 사용기</span>
--------------

최근 진행된 프로젝트를 위해서 $\textsf{python}$에 대해서 공부를 했습니다. 통계학과 수업에서 주로 $\textsf{R}$을 사용했던지라 $\textsf{python}$을 집중적으로 다루어본 것은 처음이었습니다. 이번 프로젝트는 $\textsf{R}$로 작성해놓은 코드를 $\textsf{python}$으로 고치고 DB에 직접 데이터 프레임을 올리는 작업까지 포함하고 있어서 DB를 다룰 기회가 없었던 저에게는 정말 좋은 기회였습니다. 

우선 이번 기회를 통해 느낀 것이 있습니다.

- DB 관리는 정말 까다롭다.

- 데이터 전처리에는 확실히 $\textsf{R}$이 $\textsf{python}$보다 편하다.

먼저, DB에 대해서 말해보겠습니다. 

저는 DB에 대해서는 거의 무지한 상태였습니다. 그래서 DB에 접속하는 법이라든지 테이블을 보는 방법도 몰랐습니다. HeidiSQL 프로그램(무료)을 받아서 DB에 접속도 해보고 DB 구성을 확인할 수 있었습니다.

HeidiSQL을 통해 DB에 어떤 테이블이 있는지 확인을 했으면 이제 전처리 작업을 해야하는 $\textsf{python}$ 환경으로 불러와야합니다. 

$\textsf{python}$에서  $\textsf{pandas}$ 데이터 프레임 객체를 DB에 직접 올리는 'pd.to_sql' 메소드가 존재합니다. 굉장히 편리하다고 생각할 수 있습니다. 하지만 처음 DB에 올리는 것이라면 대부분 에러가 발생할 것이라고 생각합니다. 그 이유는 다양하지만 제가 겪은 것을 토대로 말씀드리겠습니다.
- DB 옵션이 데이터 프레임의 크기에 맞지 않는 경우
- $\textsf{pandas}$ DataFrame이 Nan, Inf나 -Inf 값을 가지고 있는 경우
- DB가 $\textsf{pandas}$ DataFrame의 dtype을 인식 못하는 경우
```{python}


```
 $\textsf{R}$이 확실히 익숙한 언어라서 그런 것일 수 있지만 확실히 $\textsf{dplyr}$ 패키지는 $\textsf{pandas}$보다는 매우 직관적이고 강력한 데이터 전처리 패키지라고 생각했습니다. 


<!--stackedit_data:
eyJoaXN0b3J5IjpbODIwMTI1MjMsLTM0ODk3Nzc5OCwtNDMxNj
YwNDE5XX0=
-->