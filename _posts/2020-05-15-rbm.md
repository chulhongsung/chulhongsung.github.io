---
title: "Restricted Boltzmann Machines(RBM)"
excerpt: RBM

toc: true
toc_sticky: true
use_math: true

author_profile: true

date: 2020-05-15 19:00:00 -0000

categories: 
  - 딥러닝
  
tags:
  -  생성 모형
  -  분포 추정
---
석사과정 딥러닝 수업 막바지에 'Restricted Boltzmann Machines(RBM: 제한된 볼츠만 머신)'에 대해서 배울 기회가 있었다. 당시에 앞에서 배웠던 신경망 모형과 다르게 학습이 단순하지 않았던 제한된 볼츠만 머신을 이해하기 어려웠다. 기존의 신경망 모형들은 결과적으로 역전파 알고리즘을 통해 미분값을 계산할 수 있으면 학습이 가능했다. 하지만 볼츠만 머신은 수식의 유도와 학습에 있어서 샘플링이 필요해서 쉽지 않았다. 

이번에 제한된 볼츠만 머신이 필요로 한 문제가 발생하여 다시 책을 폈다. 내용은 Goodfellow의 'Deep learning' 책과 전북대 오일석 교수님의 '기계 학습' 책을 참고했다. 
## <span style="color:#00ADB5;"> Restricted Boltzmann Machines</span>

볼츠만 머신은 이진 벡터(${\bf x} \in \{0,1\}^d$)에 대한 확률 분포를 학습하기 위해 제안되었다. 학습된 확률분포를 통해서 새로운 데이터를 생성할 수 있습니다. 먼저, 확률분포를 모형화해야합니다. 통계학에서는 가우시안 분포나 이진 변수이면 베르누이 분포 등을 통해 확률분포를 모형화했을 수 있습니다. 하지만 볼츠만 머신은 에너지 함수라는 물리학 개념을 사용했습니다.

### <span style="color:#F7DC6F;">Energy Function</span>
볼츠만 머신에서는 확률분포를 다음의 에너지 함수를 통해서 정의한다.

$$ P({\bf x}) = \frac{\exp(-E({\bf x}))}{Z},$$

여기서 $Z$는 합이 1이 되기 위한 정규화항이고 $E({\bf x})$가 벡터 $\bf x$의 에너지를 나타내는 에너지 함수이다. 즉, $\bf x$의 에너지($E({\bf x})$)가 높을수록 $\bf x$의 확률값은 낮아진다. 에너지 함수에 대한 개념이 물리 이론에서 나왔다고 하는데 현실에서는 에너지가 높은 상태의 상태는 드물고 에너지가 낮은 안전한 상태로 있기 때문이다.

이제 에너지 함수($E$)를 모형화해야한다. 볼츠만 머신에서는 에너지 함수 $E$를 모형화하는데 신경망 모형을 사용했다. 

$$ E({\bf x}, {\bf h}) = -{\bf x^\top R x} -{\bf x^\top W h} - {\bf h^\top S h} - {\bf b^\top x} - {\bf c^\top h}.$$

제한된 볼츠만 머신은 $\bf x$와 $\bf x$, $\bf h$와 $\bf h$사이의 관계는 고려하지 않는다. 그래서 모형에 제한을 두었기 때문에 제한된 볼츠만 머신이라고 한다.

$$ E({\bf x}, {\bf h}) = -{\bf x^\top W h} - {\bf b^\top x} - {\bf c^\top h}.$$

여기서는 데이터를 $\bf x$로 표현을 했지만 다른 RBM 관련 문헌에서는 관측 가능하다(visible)고 해서 $\bf v$로 표현했다. 
<p align="center"> <img src ='https://user-images.githubusercontent.com/37679460/82027416-316f5d00-96cf-11ea-8727-fc4ab0b26fd1.png' 
width="650px" height="300px"
></p>

위의 그림을 보면 두 에너지 함수의 차이를 쉽게 확인할 수 있을 것이다. 오른쪽의 RBM은 같은 층에 있는 노드들끼리의 연결선이 없다. 



<!--stackedit_data:
eyJoaXN0b3J5IjpbNTM4MTg2MzI4LDEzNDgwMTA2NTcsMTA2MD
k3MjY1NiwyMDU0MzQzODMyLDEzMTUzMjQ0NTcsMTIyOTE0NDgz
NiwyMDI4ODMyNzMyLDUwNTQzMTk5MywtMjAxNjc3OTc0MywyMD
MwNTg1MjEsNzc3NDg5Njc0XX0=
-->